package org.neteinstein.family.domain.model

/** User-selectable app appearance: follow the OS setting, or force light/dark regardless of it. */
enum class ThemeMode {
    SYSTEM,
    LIGHT,
    DARK,
}
